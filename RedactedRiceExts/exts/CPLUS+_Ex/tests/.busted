-- Busted configuration file
return {
  default = {
    verbose = true,
    coverage = true,
    lpath = "../scripts/?.lua",
    output = "plainTerminal",
    ROOT = {"cplus_plus_ex_spec.lua"}
  },
  coverage = {
    verbose = true,
    coverage = true,
    lpath = "../scripts/?.lua",
    output = "plainTerminal"
  }
}
