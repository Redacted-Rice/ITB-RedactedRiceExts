-- Busted configuration for CPLUS+ Extension
-- Run from CPLUS+_Ex folder with: busted
return {
  _all = {
    lua = "lua",
    verbose = true,
    coverage = false,
    output = "plainTerminal",
    helper = "spec/clear_coverage.lua",
    lpath = "scripts/?.lua;scripts/?/init.lua;spec/?.lua;spec/?/init.lua"
  },
  default = {
    ROOT = {"spec"},
    pattern = "_spec",
  }
}
