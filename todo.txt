---------- Memhack ---------

For 1.1 Release:
3. Try reverse engineering more things and add to memhack if possible
	a. mech innate upgrades
	b. Deploy zone (probably point list pointer in mission)
	c. bump damage


1.2 Release:
1. Steam & GoG (Epic?) offset fixes
	a. Maybe could use known vtables (like pilot) to determine GoG vs Steam and then use the corresponding offset
	b. Current fields needing this: Score


---------- CPLUS+ Ex ---------

For 1.1 Release:
3. Pool based exclusions (testing)


1.2 Release:
1. Squad bases exclusions
2. Modify to allow for selecting specific skills easily in code and selecting more than 2
3. consider using bulk reading for state capture to avoid multiple reads near each other as optimization


---------- Mech Upgrades Ex ---------

Figure out a name to call this
Use CPLUS+ to determine when upgrades are set and then undo them to replace with custom ones
Create a similar UI/structure to CPLUS+ to allow choosing upgrades, frequency and allowing more to be added
